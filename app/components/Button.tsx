'use client';
import React from 'react';

const Button = () => {
  return (
    <div>
      <button onClick={() => console.log('Button clicked')}>Button</button>
    </div>
  );
};

export default Button;